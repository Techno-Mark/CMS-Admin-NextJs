const commonPrefix = "organization/static-content-block"

export const staticContentBlock = {
  list: `${commonPrefix}/list`,
  getById: `${commonPrefix}/getById`,
  saveAndUpdate: `${commonPrefix}/saveAndUpdate`
}
