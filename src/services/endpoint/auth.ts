const commonPrefix = "auth"

export const authnetication = {
  forgot_password: `${commonPrefix}/forgot-password`,
  reset_password: `${commonPrefix}/reset-password`,
  user_permission_data: `${commonPrefix}/get-role-permissions`
}
